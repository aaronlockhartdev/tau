{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The GUI's entire Tauri surface is the protocol: one invoke (the app's `tau_command`, allowed by default for app-registered commands) and a listener for the coalesced `tau://event` stream — nothing else needs a core permission.",
  "windows": ["main"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "dialog:allow-open",
    "pilot:default"
  ]
}
