# Development example config: the hosted vLLM test endpoint (switched 2026-09-18).
# Copy to ~/.config/tau/config.toml (system) or .tau/config.toml (project).
# The model is a thinking model: keep test generations small (max_output_tokens).
[providers.dev]
base_url = "https://llms.aaronlockhart.dev/v1"
key_env = ""

[providers.dev.models."qwen3.8-27b"]

[om]
om_model = "qwen3.8-27b"
